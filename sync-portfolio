#!/bin/sh
# Cybersecurity Portfolio Sync Engine — interactive multi-platform launcher.
set -eu

cd "$(dirname "$0")"
if [ ! -x .venv/bin/python ]; then
  printf 'Setup has not been run yet. Run: ./setup\n' >&2
  exit 1
fi
. .venv/bin/activate
exec python portfolio.py sync "$@"
